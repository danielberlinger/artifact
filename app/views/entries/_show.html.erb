<p id="notice"><%= notice %></p>
<% if user_signed_in? %>
<%= link_to 'Edit', edit_entry_path(@entry) %> |
<%= link_to 'Back', entries_path %> |
<a href="https://artifact.medivo.io/entries/161" target="_blank">Markup help</a> ||
<%= access_token_status(@entry) %>
<% end %>

<p>
  <strong><%= @entry.title %></strong>
</p>

<p>
  <%= spiffify(@entry.content) %>
</p>


<% if user_signed_in? %>
<%= link_to 'Edit', edit_entry_path(@entry) %> |
<%= link_to 'Back', entries_path %> |
<a href="https://artifact.medivo.io/entries/161" target="_blank">Markup help</a>

<div id="revisions">
  <strong>Revisions:</strong>
  <ul>
    <li><%= link_to "Current version", entry_path(@entry) %></li>
    <%= get_sorted_versions(@entry) %>
  </ul>
</div>
<% end %>
